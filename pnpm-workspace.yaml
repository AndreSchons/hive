packages:
  - "apps/*"
  - "packages/*"
  - "tools/*"

# pnpm bloqueia scripts de postinstall por padrao. Estes tres precisam rodar:
# electron baixa o binario, better-sqlite3 e esbuild compilam nativo.
allowBuilds:
  better-sqlite3: true
  electron: true
  esbuild: true
